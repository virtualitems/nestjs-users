# System Domain Entities

```mermaid
mindmap
    root((app))
        auth
            userGroups
                config
                jwt
                mikro
            users
                config
                jwt
                mikro
        config
        jwt
        mikro
        persons
            users
```

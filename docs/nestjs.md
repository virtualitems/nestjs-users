# System Domain Entities

```mermaid
mindmap
    root((app))
        auth
            config
            jwt
            mikro
        persons
            config
            jwt
            mikro
        auth-persons
            auth
            persons
            config
            jwt
            mikro
```
